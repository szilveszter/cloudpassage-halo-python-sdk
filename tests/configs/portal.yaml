defaults:
  portal: portal.cloudpassage.com
  grid: grid.cloudpassage.com
  repo: packages.cloudpassage.com
  key_id: ${HALO_API_KEY}
  secret_key: ${HALO_API_SECRET_KEY}
  ro_key_id:
  ro_secret_key:
  api_hostname: ${HALO_API_HOSTNAME}
  api_port: ${HALO_API_PORT}
